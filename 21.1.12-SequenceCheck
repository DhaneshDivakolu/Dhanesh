public class SequenceCheck {
	public boolean sequenceCheck(int[] arr) {
		int o=0,t=0,th=0;
		for(int i=0;i<arr.length;i++)
			{
				if(arr[i]==1)
					o++;
				else if(arr[i]==2)
					t++;
				else if(arr[i]==3)
					th++;
			}
		if(o>0 && t>0 && th>0)
			return true;
		else
			return false;
	}
}
